function dom(){
	
	let url= document.URL;
	
	let anchors = document.anchors;  // Returns all <a> elements that have a name attribute.
let uri = document.baseURI; // Returns the absolute base URI of the document.
let body = document.body; // Returns the <body> element.
let cookie = document.cookie; //Returns the document's cookie.
let doctype = document.doctype; //Returns the document's doctype.
let element = document.documentElement; //Returns the <html> element.

}